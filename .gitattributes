# .ibt: no blanket LFS — small telemetry files stay normal git blobs and push as full files.
# Files >= LFS_IBT_MIN_BYTES (see githooks/pre-commit) get git lfs track added per path.
githooks/pre-commit text eol=lf
