__pycache__/
*.pyc

# IBT telemetry files (binary, too large for GitHub)
ibtfiles/
*.ibt

# Large telemetry (MoTeC .ld); use Git LFS if you need to track these
data/Ferraridata/
*.ld

# Generated solver output — keep data/ JSON (aero maps, track profiles)
/*.json
!data/**/*.json
!validation/*.json

# npm artifacts (should never be here — Python project)
node_modules/
package.json
package-lock.json

# Scratch/debug artifacts
_git_push*.py
_git_push_result*.txt
_test_*.txt
_test_*.py
_syntax_check*.py
_commit_msg*.txt
_fix_*.py
_git_*.py

# Debug/workflow temp files
*_output.txt
stdout.txt
stderr.txt
run_output.txt
pytest.txt
commit_msg*.txt
git_*.txt
ahead.txt
cl.txt
git_cal.txt

# Generated solver output files
*.sto
output_*.sto

# Temporary binary files
tmp_*.bin

# Runtime/application state
.ioptimal_app/

# Solver/pipeline output artifacts
tmp/
bundles/
outputs/
output/*.json
output/*/

# Repo indexing tools
repomix-output.xml

# One-time cleanup scripts
run_filter.sh
remove_large_file.sh

# Parsed aero map cache (regenerated from xlsx)
data/aeromaps_parsed/

# (Audit/documentation files are tracked — removed from ignore list)
# These are source-controlled documentation, not generated artifacts:
# AUDIT_REPORT.md, ENGINEERING_AUDIT.md, CLI_GUIDE.md, deep_audit_report_*.md

# IDE / editor artifacts
.vscode/
.idea/
*.swp
*.swo
*~

# OS artifacts
.DS_Store
Thumbs.db

# Virtual environments
.venv/
venv/
env/

# Distribution / build
dist/
build/
*.egg-info/

# PyInstaller
*.spec

# Auto-generated empirical model files — never merge, always regenerate
data/learnings/models/*_empirical.json

# Auto-generated runtime data — conflict-prone, always regenerate locally
data/garage_models/**/*.json
data/tracks/*.json
data/learnings/index.json
